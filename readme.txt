#### .WEBP fixer ####

**Please run install-setup.ps1 as Admin FIRST**

This will ensure that the fixer script is installed to your machine, and that the file association can work correctly.

Once installed, there will be a registry flag created. This will allow you to use this script again after install to change config settings. You can also use the uninstall.ps1 script to reverse all this.